
{
  "name": "sacred-sound-monorepo",
  "private": true,
  "workspaces": [
    "next-frontend",
    "old-frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=next-frontend",
    "build": "npm run build --workspace=next-frontend",
    "start": "npm run start --workspace=next-frontend"
  }
}
