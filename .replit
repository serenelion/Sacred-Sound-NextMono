
run = "npm run dev"
hidden = [".build", ".config"]

[nix]
channel = "stable-23_05"

[deployment]
run = ["sh", "-c", "cd next-frontend && npm run start"]
build = ["sh", "-c", "cd next-frontend && NODE_ENV=production npm install && npm run build"]
deploymentTarget = "cloudrun"
