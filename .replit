
run = "npm run dev"
hidden = [".build", ".config"]

[nix]
channel = "stable-23_05"

[deployment]
run = ["sh", "-c", "cd next-frontend && npm run start"]
build = ["sh", "-c", "npm install && cd next-frontend && npm install && npm run build"]
deploymentTarget = "cloudrun"
